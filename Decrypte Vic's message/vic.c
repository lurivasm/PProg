#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "message.h"
#include "interface.h"

void main(void){
  char **map;
  Interface *title, *game;
  int size[2];

  map = create_map("vic.txt", size);
  if(map == )
}
